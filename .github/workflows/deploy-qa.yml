name: Deploy ORC App to VPS

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy ORC to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          timeout: 30s
          command_timeout: 15m
          script: |
            set -euo pipefail
            echo "🚀 Deploying ORC app to VPS..."
            cd ~/QAProject
            # đảm bảo luôn chạy được
            chmod +x ./deployQA.sh || true
            dos2unix ./deployQA.sh 2>/dev/null || true
            bash -lc "./deployQA.sh"
            echo "✅ Deployment completed successfully!"l